import React from 'react';
import ReactDOM from 'react-dom';
import App from './App.js';

ReactDOM.render(
  <App />, //lo que quiero renderizar
  document.getElementById('root') //a donde lo quiero renderizar
  );